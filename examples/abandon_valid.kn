# abandon: non-catchable process termination for invariant violations

def check_positive(n: int) -> int {
    if n <= 0 {
        abandon
    }
    return n
}

def safe_divide(a: int, b: int) -> int {
    if b == 0 {
        abandon
    }
    return a / b
}

def main() -> void {
    x: int = check_positive(5)
    y: int = safe_divide(10, 2)
    println(x)
    println(y)
}
