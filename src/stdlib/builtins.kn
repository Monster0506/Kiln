# Kiln stdlib: builtin types and functions.
# The compiler provides all @builtin implementations.

struct VecIter[T] {
    vec: Vec[T]
    index: int
}

@builtin
struct Vec[T] {
    def new() -> Vec[T]
    def add(item: T) -> void
    def len() -> int
    def get(index: int) -> T
    def set(index: int, item: T) -> void
    def remove(index: int) -> T
    def clear() -> void
}

@builtin
@impure
def __kiln_print(s: str) -> void

@builtin
@impure
def __kiln_println(s: str) -> void

@builtin
def __kiln_int_to_str(n: int) -> str

@builtin
def __kiln_float_to_str(f: float) -> str

@builtin
def __kiln_bool_to_str(b: bool) -> str

@builtin
def __kiln_str_concat(a: str, b: str) -> str

@builtin
def __kiln_str_byte_len(s: str) -> int

@builtin
def __kiln_str_codepoint_len(s: str) -> int

@builtin
def __kiln_str_char_at(s: str, i: int) -> str

@builtin
def __kiln_str_byte_at(s: str, i: int) -> int

@builtin
def __kiln_str_slice(s: str, start: int, end: int) -> str

@builtin
def __kiln_str_starts_with(s: str, prefix: str) -> bool

@builtin
def __kiln_str_ends_with(s: str, suffix: str) -> bool

@builtin
def __kiln_str_contains(s: str, needle: str) -> bool

@builtin
def __kiln_str_find(s: str, needle: str) -> int

@builtin
def __kiln_str_find_from(s: str, needle: str, start: int) -> int

@builtin
def __kiln_str_to_upper(s: str) -> str

@builtin
def __kiln_str_to_lower(s: str) -> str

@builtin
def __kiln_str_reverse(s: str) -> str

@builtin
def __kiln_str_trim(s: str) -> str

@builtin
def __kiln_str_trim_start(s: str) -> str

@builtin
def __kiln_str_trim_end(s: str) -> str

@builtin
def __kiln_str_replace(s: str, from: str, to: str) -> str

@builtin
def __kiln_str_repeat(s: str, n: int) -> str

@builtin
def __kiln_str_split(s: str, sep: str) -> Vec[str]

@builtin
def __kiln_str_split_whitespace(s: str) -> Vec[str]

@builtin
def __kiln_str_chars(s: str) -> Vec[str]

@builtin
def __kiln_str_bytes_vec(s: str) -> Vec[int]

@builtin
def __kiln_str_pad_start(s: str, width: int, pad: str) -> str

@builtin
def __kiln_str_pad_end(s: str, width: int, pad: str) -> str

@builtin
def __kiln_str_remove_prefix(s: str, prefix: str) -> str

@builtin
def __kiln_str_remove_suffix(s: str, suffix: str) -> str

@builtin
def __kiln_str_parse_int(s: str) -> Option[int]

@builtin
def __kiln_str_parse_float(s: str) -> Option[float]

@builtin
@impure
def panic(msg: str) -> void

@builtin
@impure
def assert(cond: bool) -> void

@builtin
@impure
def clock_ms() -> int

@builtin
struct Exception {}

# Mutex[T] -- interior-mutability wrapper.
# Declared invariant in T by the compiler (see variance pass in analyzer/mod.rs).
@builtin
struct Mutex[T] {
    def new(value: T) -> Mutex[T]
    def lock() -> T
    def unlock(value: T) -> void
}

@builtin
struct RangeIter {
    current: int
    limit:   int
}

@builtin
struct Range {
    start: int
    end:   int
}

# Path -- filesystem path with file I/O methods.
# Internally stored as a str; Path_new is a no-op passthrough on the C side.
@builtin
@impure
struct Path {
    def new(p: str) -> Path
    def read_text() -> Result[str, str]
    def write_text(content: str) -> Result[int, str]
    def append_text(content: str) -> Result[int, str]
    def exists() -> bool
    def is_file() -> bool
    def is_dir() -> bool
    def delete() -> Result[int, str]
    def create_dir() -> Result[int, str]
    def create_dir_all() -> Result[int, str]
    def list_dir() -> Result[Vec[str], str]
    def file_size() -> Result[int, str]
    def copy_to(dst: str) -> Result[int, str]
    def rename_to(dst: str) -> Result[int, str]
    def join(part: str) -> Path
    def basename() -> str
    def dirname() -> Path
    def extension() -> str
    def stem() -> str
    def to_str() -> str
}

@builtin
@impure
def path_cwd() -> str

@builtin
@impure
def path_temp_dir() -> str

@builtin
def __kiln_math_floor(x: float) -> float

@builtin
def __kiln_math_ceil(x: float) -> float

@builtin
def __kiln_math_round(x: float) -> float

@builtin
def __kiln_math_trunc(x: float) -> float

@builtin
def __kiln_math_fract(x: float) -> float

@builtin
def __kiln_math_sqrt(x: float) -> float

@builtin
def __kiln_math_cbrt(x: float) -> float

@builtin
def __kiln_math_pow(x: float, y: float) -> float

@builtin
def __kiln_math_powi(x: float, n: int) -> float

@builtin
def __kiln_math_hypot(x: float, y: float) -> float

@builtin
def __kiln_math_exp(x: float) -> float

@builtin
def __kiln_math_exp2(x: float) -> float

@builtin
def __kiln_math_ln(x: float) -> float

@builtin
def __kiln_math_log2(x: float) -> float

@builtin
def __kiln_math_log10(x: float) -> float

@builtin
def __kiln_math_log(x: float, base: float) -> float

@builtin
def __kiln_math_sin(x: float) -> float

@builtin
def __kiln_math_cos(x: float) -> float

@builtin
def __kiln_math_tan(x: float) -> float

@builtin
def __kiln_math_asin(x: float) -> float

@builtin
def __kiln_math_acos(x: float) -> float

@builtin
def __kiln_math_atan(x: float) -> float

@builtin
def __kiln_math_atan2(y: float, x: float) -> float

@builtin
def __kiln_math_sinh(x: float) -> float

@builtin
def __kiln_math_cosh(x: float) -> float

@builtin
def __kiln_math_tanh(x: float) -> float

@builtin
def __kiln_math_asinh(x: float) -> float

@builtin
def __kiln_math_acosh(x: float) -> float

@builtin
def __kiln_math_atanh(x: float) -> float

@builtin
def __kiln_math_to_degrees(x: float) -> float

@builtin
def __kiln_math_to_radians(x: float) -> float

@builtin
def __kiln_math_fabs(x: float) -> float

@builtin
def __kiln_math_fmin(x: float, y: float) -> float

@builtin
def __kiln_math_fmax(x: float, y: float) -> float

@builtin
def __kiln_math_fclamp(x: float, lo: float, hi: float) -> float

@builtin
def __kiln_math_is_nan(x: float) -> bool

@builtin
def __kiln_math_is_infinite(x: float) -> bool

@builtin
def __kiln_math_is_finite(x: float) -> bool

@builtin
def __kiln_math_isclose(a: float, b: float, rel_tol: float, abs_tol: float) -> bool

@builtin
def __kiln_math_ipow(base: int, exp: int) -> int

@builtin
def __kiln_math_gcd(a: int, b: int) -> int

@builtin
def __kiln_math_lcm(a: int, b: int) -> int

@builtin
def __kiln_math_imin(a: int, b: int) -> int

@builtin
def __kiln_math_imax(a: int, b: int) -> int

@builtin
def __kiln_math_iclamp(x: int, lo: int, hi: int) -> int

@builtin
def __kiln_math_factorial(n: int) -> int

@builtin
def __kiln_math_comb(n: int, k: int) -> int

@builtin
def __kiln_math_perm(n: int, k: int) -> int

export {
    VecIter, Vec,
    __kiln_print, __kiln_println,
    __kiln_int_to_str, __kiln_float_to_str, __kiln_bool_to_str,
    __kiln_str_concat,
    __kiln_str_byte_len, __kiln_str_codepoint_len,
    __kiln_str_char_at, __kiln_str_byte_at,
    __kiln_str_slice, __kiln_str_starts_with, __kiln_str_ends_with,
    __kiln_str_contains, __kiln_str_find, __kiln_str_find_from,
    __kiln_str_to_upper, __kiln_str_to_lower, __kiln_str_reverse,
    __kiln_str_trim, __kiln_str_trim_start, __kiln_str_trim_end,
    __kiln_str_replace, __kiln_str_repeat,
    __kiln_str_split, __kiln_str_split_whitespace,
    __kiln_str_chars, __kiln_str_bytes_vec,
    __kiln_str_pad_start, __kiln_str_pad_end,
    __kiln_str_remove_prefix, __kiln_str_remove_suffix,
    __kiln_str_parse_int, __kiln_str_parse_float,
    panic, assert, clock_ms,
    Exception,
    Mutex,
    RangeIter, Range,
    __kiln_math_floor, __kiln_math_ceil, __kiln_math_round, __kiln_math_trunc, __kiln_math_fract,
    __kiln_math_sqrt, __kiln_math_cbrt, __kiln_math_pow, __kiln_math_powi, __kiln_math_hypot,
    __kiln_math_exp, __kiln_math_exp2, __kiln_math_ln, __kiln_math_log2, __kiln_math_log10, __kiln_math_log,
    __kiln_math_sin, __kiln_math_cos, __kiln_math_tan,
    __kiln_math_asin, __kiln_math_acos, __kiln_math_atan, __kiln_math_atan2,
    __kiln_math_sinh, __kiln_math_cosh, __kiln_math_tanh,
    __kiln_math_asinh, __kiln_math_acosh, __kiln_math_atanh,
    __kiln_math_to_degrees, __kiln_math_to_radians,
    __kiln_math_fabs, __kiln_math_fmin, __kiln_math_fmax, __kiln_math_fclamp,
    __kiln_math_is_nan, __kiln_math_is_infinite, __kiln_math_is_finite, __kiln_math_isclose,
    __kiln_math_ipow, __kiln_math_gcd, __kiln_math_lcm,
    __kiln_math_imin, __kiln_math_imax, __kiln_math_iclamp,
    __kiln_math_factorial, __kiln_math_comb, __kiln_math_perm,
    Path, path_cwd, path_temp_dir
}

